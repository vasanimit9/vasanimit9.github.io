caches.open('vasanimit9.github.io.6')
       .then(cache => {
        cache.delete('/');
      });
    caches.open('vasanimit9.github.io.7')
       .then(cache => {
        cache.delete('/');
    });
